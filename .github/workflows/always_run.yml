name: 24/7 Scheduled Script
on:
  schedule:
    - cron: '*/5 * * * *' # Runs every 5 minutes
jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Code
        run: python main.py
